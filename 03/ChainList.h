//#include <stdlib.h>
//
//typedef struct Node {
//    DATA data;
//    struct Node *next;
//} ChainList;
//
//ChainList *addEnd(ChainList *head, DATA data);  //添加结点到链表末尾
//ChainList *addFirst(ChainList *head, DATA data);  //添加结点到链表首部
//ChainList *find(ChainList *head, char *key); //按关键字在链表中查找内容
//ChainList *insert(ChainList *head, char *findKey, DATA data);  //插入结点到链表指定位置
//int delete(ChainList *head, char *key);//删除指定关键字的结点
//int length(ChainList *head);//获取链表结点数量
